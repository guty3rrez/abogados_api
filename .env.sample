# Variables de Entorno para la Aplicación de Reservas

# Entorno de Node.js (development, production, test)
NODE_ENV=development

# Puerto para el servidor HTTP (si no se usa HTTPS)
PORT_HTTP=3000

# Puerto para el servidor HTTPS (si se usa el argumento 'https')
PORT_HTTPS=443

# URL de conexión a la base de datos PostgreSQL
# Ejemplo: postgresql://usuario:contraseña@host:puerto/basedatos
# Asegúrate de que esté correctamente formateada.
DATABASE_URL=postgresql://postgres:mysecretpassword@localhost:5432/reservas_db

# --- Configuración SSL (solo para modo HTTPS) ---
# Ruta al archivo de la clave privada SSL (relativa a la raíz del proyecto)
SSL_KEY_PATH=key.pem

# Ruta al archivo del certificado SSL (relativa a la raíz del proyecto)
SSL_CERT_PATH=cert.pem

# --- Otras configuraciones (si fueran necesarias) ---
# Ejemplo: API_KEY_EXTERNA=abcdef123456